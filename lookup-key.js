/* The course lookup key, embedded so nobody has to type it.
 *
 * Paste the key from golfcourseapi.com between the quotes below, commit, and
 * every user picks it up on their next reload — guests and Google accounts
 * alike. Nobody sees a key box; searching just works.
 *
 * Only you can change it, because only you can commit to this repository.
 *
 * One thing to be aware of: a public repository means this key is publicly
 * readable. It is a free-tier key for course lookups, so the exposure is
 * somebody else using your search quota, not access to your data. If that ever
 * matters, rotate the key here and commit.
 */
self.COURSE_LOOKUP_KEY = "";
